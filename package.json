{
  "name": "lerna-yakbak-monorepo",
  "version": "1.0.0",
  "description": "yakbak implementation for nestjs",
  "license": "MIT",
  "scripts": {
    "start": "lerna run --stream --scope @ball6847/yakbak-nestjs-app start",
    "bootstrap": "lerna bootstrap",
    "build:app": "lerna run --stream --scope @ball6847/yakbak-nestjs-app build",
    "build:yakbak": "lerna run --stream --scope @ball6847/yakbak-nestjs build"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
